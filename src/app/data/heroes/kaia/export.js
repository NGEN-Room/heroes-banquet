import chObject from './chObject';
import chSpells from './chSpells';
import chActions from './chActions';

const kaia = {
  ...chObject,
  spells: chSpells,
  actions: chActions
};

export default kaia;